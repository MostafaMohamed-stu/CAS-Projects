// StudentProfileRepository was removed as part of migration to StudentExtension.
// The original repository implementation lived here but references to StudentProfile
// were causing build errors after the model removal. Kept placeholder file to
// preserve history. If you need the full repository reimplemented against
// StudentExtension, I can port its methods now.

